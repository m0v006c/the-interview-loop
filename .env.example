# Anthropic API Key (REQUIRED)
# Get your key at: https://console.anthropic.com
# The server.js proxy uses this to authenticate with the Anthropic API.
# Your key never reaches the browser — it stays server-side.
ANTHROPIC_API_KEY=sk-ant-your-key-here
